// Basic if-else structure
let bool = true;
let bool_2 = 2 + 2; // 4
// 0, "", null, undefined, NaN --> false
true
"false" // true

if(/* condtion */ bool_2) {
    // if condition code block
} else {
    // else code block
}

// Extended if-else structure
if() {}
else if () {} 
else if () {} 
else if () {} 
else if () {} 
else {}

// Switch structure
let myVar = 3;
switch (myVar) {
    case 1:
        // code block
        break;
    case 2:
        // code block
        break;
    case 3:
        // code block
        break;
    default:
        // code block
        break;
}